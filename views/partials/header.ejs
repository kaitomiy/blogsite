<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.4.1/semantic.min.css"
    />
    <link rel="stylesheet" href="/stylesheets/app.css" />
    <title>Blog</title>
  </head>
  <body>
    <div class="ui main container">
      <div class="ui fixed inverted menu">
        <a class="active item"><i class="comment icon"></i>Blog Site</a>
        <a href="/" class="item"><i class="home icon"></i>Home</a>
        <a href="/blogs/new" class="item"
          ><i class="pencil alternate icon"></i>New Post</a
        >
        <div class="right menu">
          <% if(!currentUser){ %>
          <a href="/signup" class="item">Sign Up</a>
          <a href="/login" class="item">Login</a>
          <% } else { %>
          <a href="#" class="item">Signed In As <%= currentUser.username %></a>
          <a href="/logout" class="item">Logout</a>
          <% } %>
        </div>
      </div>
    </div>

    <div class="ui container">
      <% if(error && error.length > 0) { %>
      <div class="ui error message"><p><%= error %></p></div>
      <% } %> <% if(success && success.length > 0) { %>
      <div class="ui success message"><p><%= success %></p></div>
      <% } %>
    </div>
  </body>
</html>
